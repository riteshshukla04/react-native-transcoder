///
/// PlanPath.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::transcoder {

  /**
   * An enum which can be represented as a JavaScript union (PlanPath).
   */
  enum class PlanPath {
    PROBE_ONLY      SWIFT_NAME(probeOnly) = 0,
    METADATA_REMUX      SWIFT_NAME(metadataRemux) = 1,
    PACKET_REMUX      SWIFT_NAME(packetRemux) = 2,
    DECODE_ONLY      SWIFT_NAME(decodeOnly) = 3,
    ENCODE_ONLY      SWIFT_NAME(encodeOnly) = 4,
    FULL_TRANSCODE      SWIFT_NAME(fullTranscode) = 5,
  } CLOSED_ENUM;

} // namespace margelo::nitro::transcoder

namespace margelo::nitro {

  // C++ PlanPath <> JS PlanPath (union)
  template <>
  struct JSIConverter<margelo::nitro::transcoder::PlanPath> final {
    static inline margelo::nitro::transcoder::PlanPath fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("probe-only"): return margelo::nitro::transcoder::PlanPath::PROBE_ONLY;
        case hashString("metadata-remux"): return margelo::nitro::transcoder::PlanPath::METADATA_REMUX;
        case hashString("packet-remux"): return margelo::nitro::transcoder::PlanPath::PACKET_REMUX;
        case hashString("decode-only"): return margelo::nitro::transcoder::PlanPath::DECODE_ONLY;
        case hashString("encode-only"): return margelo::nitro::transcoder::PlanPath::ENCODE_ONLY;
        case hashString("full-transcode"): return margelo::nitro::transcoder::PlanPath::FULL_TRANSCODE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum PlanPath - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::transcoder::PlanPath arg) {
      switch (arg) {
        case margelo::nitro::transcoder::PlanPath::PROBE_ONLY: return JSIConverter<std::string>::toJSI(runtime, "probe-only");
        case margelo::nitro::transcoder::PlanPath::METADATA_REMUX: return JSIConverter<std::string>::toJSI(runtime, "metadata-remux");
        case margelo::nitro::transcoder::PlanPath::PACKET_REMUX: return JSIConverter<std::string>::toJSI(runtime, "packet-remux");
        case margelo::nitro::transcoder::PlanPath::DECODE_ONLY: return JSIConverter<std::string>::toJSI(runtime, "decode-only");
        case margelo::nitro::transcoder::PlanPath::ENCODE_ONLY: return JSIConverter<std::string>::toJSI(runtime, "encode-only");
        case margelo::nitro::transcoder::PlanPath::FULL_TRANSCODE: return JSIConverter<std::string>::toJSI(runtime, "full-transcode");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert PlanPath to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("probe-only"):
        case hashString("metadata-remux"):
        case hashString("packet-remux"):
        case hashString("decode-only"):
        case hashString("encode-only"):
        case hashString("full-transcode"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
