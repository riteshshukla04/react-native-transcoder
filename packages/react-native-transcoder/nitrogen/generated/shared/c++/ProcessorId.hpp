///
/// ProcessorId.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::transcoder {

  /**
   * An enum which can be represented as a JavaScript union (ProcessorId).
   */
  enum class ProcessorId {
    SAMPLE_FORMAT_CONVERT      SWIFT_NAME(sampleFormatConvert) = 0,
    RESAMPLE      SWIFT_NAME(resample) = 1,
    CHANNEL_MAP      SWIFT_NAME(channelMap) = 2,
    TRIM      SWIFT_NAME(trim) = 3,
    CONCAT      SWIFT_NAME(concat) = 4,
    GAIN      SWIFT_NAME(gain) = 5,
    FADE      SWIFT_NAME(fade) = 6,
    LOUDNESS_MEASURE      SWIFT_NAME(loudnessMeasure) = 7,
    LOUDNESS_NORMALIZE      SWIFT_NAME(loudnessNormalize) = 8,
    LIMITER      SWIFT_NAME(limiter) = 9,
    DITHER      SWIFT_NAME(dither) = 10,
  } CLOSED_ENUM;

} // namespace margelo::nitro::transcoder

namespace margelo::nitro {

  // C++ ProcessorId <> JS ProcessorId (union)
  template <>
  struct JSIConverter<margelo::nitro::transcoder::ProcessorId> final {
    static inline margelo::nitro::transcoder::ProcessorId fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("sample-format-convert"): return margelo::nitro::transcoder::ProcessorId::SAMPLE_FORMAT_CONVERT;
        case hashString("resample"): return margelo::nitro::transcoder::ProcessorId::RESAMPLE;
        case hashString("channel-map"): return margelo::nitro::transcoder::ProcessorId::CHANNEL_MAP;
        case hashString("trim"): return margelo::nitro::transcoder::ProcessorId::TRIM;
        case hashString("concat"): return margelo::nitro::transcoder::ProcessorId::CONCAT;
        case hashString("gain"): return margelo::nitro::transcoder::ProcessorId::GAIN;
        case hashString("fade"): return margelo::nitro::transcoder::ProcessorId::FADE;
        case hashString("loudness-measure"): return margelo::nitro::transcoder::ProcessorId::LOUDNESS_MEASURE;
        case hashString("loudness-normalize"): return margelo::nitro::transcoder::ProcessorId::LOUDNESS_NORMALIZE;
        case hashString("limiter"): return margelo::nitro::transcoder::ProcessorId::LIMITER;
        case hashString("dither"): return margelo::nitro::transcoder::ProcessorId::DITHER;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum ProcessorId - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::transcoder::ProcessorId arg) {
      switch (arg) {
        case margelo::nitro::transcoder::ProcessorId::SAMPLE_FORMAT_CONVERT: return JSIConverter<std::string>::toJSI(runtime, "sample-format-convert");
        case margelo::nitro::transcoder::ProcessorId::RESAMPLE: return JSIConverter<std::string>::toJSI(runtime, "resample");
        case margelo::nitro::transcoder::ProcessorId::CHANNEL_MAP: return JSIConverter<std::string>::toJSI(runtime, "channel-map");
        case margelo::nitro::transcoder::ProcessorId::TRIM: return JSIConverter<std::string>::toJSI(runtime, "trim");
        case margelo::nitro::transcoder::ProcessorId::CONCAT: return JSIConverter<std::string>::toJSI(runtime, "concat");
        case margelo::nitro::transcoder::ProcessorId::GAIN: return JSIConverter<std::string>::toJSI(runtime, "gain");
        case margelo::nitro::transcoder::ProcessorId::FADE: return JSIConverter<std::string>::toJSI(runtime, "fade");
        case margelo::nitro::transcoder::ProcessorId::LOUDNESS_MEASURE: return JSIConverter<std::string>::toJSI(runtime, "loudness-measure");
        case margelo::nitro::transcoder::ProcessorId::LOUDNESS_NORMALIZE: return JSIConverter<std::string>::toJSI(runtime, "loudness-normalize");
        case margelo::nitro::transcoder::ProcessorId::LIMITER: return JSIConverter<std::string>::toJSI(runtime, "limiter");
        case margelo::nitro::transcoder::ProcessorId::DITHER: return JSIConverter<std::string>::toJSI(runtime, "dither");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert ProcessorId to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("sample-format-convert"):
        case hashString("resample"):
        case hashString("channel-map"):
        case hashString("trim"):
        case hashString("concat"):
        case hashString("gain"):
        case hashString("fade"):
        case hashString("loudness-measure"):
        case hashString("loudness-normalize"):
        case hashString("limiter"):
        case hashString("dither"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
