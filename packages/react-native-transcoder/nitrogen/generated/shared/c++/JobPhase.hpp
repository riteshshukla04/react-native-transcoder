///
/// JobPhase.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::transcoder {

  /**
   * An enum which can be represented as a JavaScript union (JobPhase).
   */
  enum class JobPhase {
    STARTING      SWIFT_NAME(starting) = 0,
    STAGING_SOURCE      SWIFT_NAME(stagingSource) = 1,
    READING      SWIFT_NAME(reading) = 2,
    TRANSCODING      SWIFT_NAME(transcoding) = 3,
    FLUSHING      SWIFT_NAME(flushing) = 4,
    FINALIZING      SWIFT_NAME(finalizing) = 5,
    DONE      SWIFT_NAME(done) = 6,
  } CLOSED_ENUM;

} // namespace margelo::nitro::transcoder

namespace margelo::nitro {

  // C++ JobPhase <> JS JobPhase (union)
  template <>
  struct JSIConverter<margelo::nitro::transcoder::JobPhase> final {
    static inline margelo::nitro::transcoder::JobPhase fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("starting"): return margelo::nitro::transcoder::JobPhase::STARTING;
        case hashString("staging-source"): return margelo::nitro::transcoder::JobPhase::STAGING_SOURCE;
        case hashString("reading"): return margelo::nitro::transcoder::JobPhase::READING;
        case hashString("transcoding"): return margelo::nitro::transcoder::JobPhase::TRANSCODING;
        case hashString("flushing"): return margelo::nitro::transcoder::JobPhase::FLUSHING;
        case hashString("finalizing"): return margelo::nitro::transcoder::JobPhase::FINALIZING;
        case hashString("done"): return margelo::nitro::transcoder::JobPhase::DONE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum JobPhase - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::transcoder::JobPhase arg) {
      switch (arg) {
        case margelo::nitro::transcoder::JobPhase::STARTING: return JSIConverter<std::string>::toJSI(runtime, "starting");
        case margelo::nitro::transcoder::JobPhase::STAGING_SOURCE: return JSIConverter<std::string>::toJSI(runtime, "staging-source");
        case margelo::nitro::transcoder::JobPhase::READING: return JSIConverter<std::string>::toJSI(runtime, "reading");
        case margelo::nitro::transcoder::JobPhase::TRANSCODING: return JSIConverter<std::string>::toJSI(runtime, "transcoding");
        case margelo::nitro::transcoder::JobPhase::FLUSHING: return JSIConverter<std::string>::toJSI(runtime, "flushing");
        case margelo::nitro::transcoder::JobPhase::FINALIZING: return JSIConverter<std::string>::toJSI(runtime, "finalizing");
        case margelo::nitro::transcoder::JobPhase::DONE: return JSIConverter<std::string>::toJSI(runtime, "done");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert JobPhase to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("starting"):
        case hashString("staging-source"):
        case hashString("reading"):
        case hashString("transcoding"):
        case hashString("flushing"):
        case hashString("finalizing"):
        case hashString("done"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
